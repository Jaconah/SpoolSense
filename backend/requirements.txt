# Core Framework
fastapi==0.129.0
uvicorn[standard]==0.41.0

# Database
sqlalchemy==2.0.46
alembic==1.18.4

# Data Validation & Settings
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.1

# HTTP Client (Discord webhooks, Turnstile validation)
httpx==0.28.1

# Required by FastAPI for UploadFile / multipart form data (CSV import)
python-multipart>=0.0.9

# Background Tasks (Discord notifications)
apscheduler==3.11.2

# User-Agent parsing (device type labels for trusted devices UI)
user-agents==2.2.0

# Local GeoIP lookup — queries MaxMind GeoLite2-City.mmdb (no outbound HTTP)
geoip2>=4.0.0

# Authentication & Security
PyJWT>=2.8.0
bcrypt>=4.0.0
email-validator==2.3.0
slowapi==0.1.9
pyotp>=2.9.0
cryptography>=42.0.0

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
anyio==4.12.1
