# SpoolSense Self-Hosted — Environment Variables
# Copy this file to .env and fill in what you need.
# Everything has sensible defaults — nothing is required.

# Set this if you're using a reverse proxy or custom domain.
# If not set, CORS allows all origins (fine for direct IP:port access).
# FRONTEND_URL=https://yourdomain.com

# Optional — webhook for error/alert notifications (Discord, Slack, etc.)
# ERROR_WEBHOOK_URL=
